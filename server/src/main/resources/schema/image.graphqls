# Images Schema

type Image {
    id: ID!
    src: String
    name: String
    alt: String
    ext: String
    mime: String
    size: Int
    external: Boolean
    createdAt: DateTime
    updatedAt: DateTime
}