# Product Category schema

type ProductCategoryResponse{
    id: ID!
    name: String
    slug: String
    description: String
    children: [ChildCategory]
    image: Image
    createdBy: User
    updatedBy: User
    createdAt: DateTime
    updatedAt: DateTime
}